name: "Events"

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  events:
    if: github.actor != 'dependabot[bot]' && github.event.pull_request.user.login != 'dependabot[bot]' && !startsWith(github.head_ref, 'dependabot/')
    name: "Events & Automation"
    uses: Koudmain/.github/.github/workflows/orchestrator-events.yml@main
    permissions:
      pull-requests: write
    with:
      target-repo-url: git@gitlab.com:koudmain/koudmain-backend.git
    secrets: inherit
